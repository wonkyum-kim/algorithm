#include <vector>

using namespace std;

template<typename T>
int linear_search(vector<T>& vec, const int& len, const T& target)
{

	for (auto& i : vec)
	{
		if (target == i)
			return i;
	}
  
	return -1;
}
